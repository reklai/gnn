package pages

import (
	"gnn/internal/components"
	"gnn/internal/content"
)

templ Bar(site content.SiteContent) {
	@components.Layout(site, "Bayside Bar", "bar") {
		<section class="page-section">
			@components.SchedulePanel(site.BarSchedule)
		</section>
	}
}
